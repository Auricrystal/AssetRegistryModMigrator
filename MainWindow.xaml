<Window x:Class="AssetRegistryModMigrator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AssetRegistryModMigrator"
        xmlns:Model="clr-namespace:AssetRegistryModMigrator.Model" 
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <HierarchicalDataTemplate DataType="{x:Type Model:DirectoryItem}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <CheckBox Click="CheckBox_Click" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked" IsThreeState="True"/>
                <TextBlock Text="{Binding Path=Name}"/>

            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate DataType="{x:Type Model:FileItem}">
            <StackPanel Orientation="Horizontal">
                <CheckBox Click="CheckBox_Click" IsChecked="{Binding Path=Checked,Mode=TwoWay}" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked"/>
                <TextBlock Text="{Binding Path=Name}" />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="FileOpen_Click"/>
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="65*"/>
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" MinWidth="200" x:Name="AssetList"  SelectedItemChanged="AssetList_SelectionChanged" TreeViewItem.Selected="TreeViewItem_Selected"/>

            <GridSplitter Grid.Column="1" Width="2" Background="Red" ResizeBehavior="PreviousAndNext"/>
            <TextBox FontSize="10" Grid.Column="2" DockPanel.Dock="Right" x:Name="JsonBox" Background="LightGray"/>
        </Grid>
    </DockPanel>
</Window>
